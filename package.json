{
  "name": "rye-sale-channel",
  "version": "1.0.0",
  "main": "web/index.js",
  "license": "UNLICENSED",
  "scripts": {
    "shopify": "shopify",
    "build": "shopify app build",
    "dev": "shopify app dev",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy",
    "format": "prettier --write web/**/*.{tsx,ts,js,jsx}"
  },
  "dependencies": {
    "@shopify/app": "3.20.1",
    "@shopify/cli": "3.20.1"
  },
  "author": "Rye",
  "devDependencies": {
    "prettier": "^2.7.1"
  }
}
